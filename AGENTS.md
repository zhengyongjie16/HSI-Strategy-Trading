## 项目简介

基于 Longbridge OpenAPI SDK for Node.js / Bun / TypeScript 的港股自动化量化交易系统。

## 项目结构

```
src/
├── index.ts      # 主入口
├── app/          # 应用组装层
├── config/       # 配置模块
├── constants/    # 全局常量定义
├── types/        # 全局公共类型定义
├── main/         # 主程序架构模块
├── core/         # 核心业务逻辑
├── services/     # 外部服务
└── utils/        # 工具模块
```

- 默认不读取或修改 `docs/` 和 `tools/`。仅当用户明确要求，或当前重构、Bug 修复直接影响其中的引用且必须检查残留时，才做有针对性的检查和修改；不要扩展到无关内容。

## 指令优先级与执行方式

- 遵循运行环境既定的系统与平台指令优先级。用户的明确要求优先于 skill 中的一般性建议；本文件的项目硬性规则不得被 skill 的通用建议弱化。
- 如果指令或 skill 冲突会导致暂停、任务未完成或偏离用户目标，指出具体文件和条款，区分明确要求与自身解释；没有实际冲突时不要输出这类说明。
- 用户表达执行意图时，直接推进并持续到目标完成，不要只给计划或等待再次授权。只有歧义会实质改变业务结果、涉及破坏性或不可逆操作，或无法判定交易安全相关的失败语义时，才提出聚焦问题；提问前先完成安全且不受该歧义影响的工作。
- 对不影响结果的常规细节，根据仓库上下文作合理推断并继续，最终说明关键假设。

## 第一性原理

从实际目标、业务事实和约束出发，先理解端到端运行路径，再选择最短且完整的实现。修复根因，不处理表面症状；不引入未请求的抽象、依赖或扩展范围。

## 代码与设计规范

- 编写、重构或审查 TypeScript 模块时，必须使用 `typescript-project-specifications` skill。
- 跨模块业务流程、状态迁移和异步协作严格遵循事件驱动架构；纯计算、配置读取和简单数据转换不要为了形式而包装成事件链路。
- 内部契约错误、配置错误和状态不变量错误采用 fail-fast。业务明确允许丢弃或重试的外部失败由所属链路处理，不得升级为 fatal；若分类无法从现有业务事实确定且会影响交易安全，必须询问。
- 禁止兼容层、临时补丁和绕过根因的方案；直接实现完整、正确的目标逻辑。
- 禁止为推测性或几乎不可达场景擅自增加 fallback 或降级。真实可达的边界，以及安全、数据丢失和交易正确性问题，不得以发生概率低为由忽略。
- 不编写仅追求覆盖率、重复实现细节或价值低的测试。可逆且低影响的修改无需新增测试；非平凡逻辑至少保留一项能直接验证关键行为的可运行检查。
- 运行与改动范围相称的验证和项目规定的必需检查。相关检查通过后，只有出现新改动、失败或未解决疑点时才扩大或重复验证。

## 代码修改流程

- 修改前追踪真实调用链和所有调用方，确认共享根因与影响范围。
- 宣告重构或 Bug 修复完成前，搜索并清理受影响的调用、导出、配置、测试、文档引用和内部连接路径；跨文件修改必须明确检查遗留依赖。`docs/` 和 `tools/` 仍按前述范围规则处理。
- 只清理由本次改动直接造成的废弃内容，不顺手重构无关代码。

## 评审标准

执行第一性原理审查。每项发现必须包含：主张、具体文件或函数证据、运行时路径（文档审查则写指令影响路径）、实际问题或潜在风险的判定依据、最小修复方案，以及所需测试或验证。证据不足时标记为潜在风险或 `not assessed`，不得列为已确认缺陷。

## Agent/teams 工作流

- 对 TypeScript、跨模块、架构或交易业务语义的审查，在协作工具可用时至少使用一名独立代理。其他任务仅在存在可独立并行的实质工作、且能节省时间或提高质量时委派；不要为单点、低风险任务制造代理开销。
- 只并行处理相互独立的工作，主代理负责整合判断、实施修改和最终验证。
- 每位审查代理必须返回：经检查的文件、已确认发现、未发现项、建议测试或验证，以及置信度。
